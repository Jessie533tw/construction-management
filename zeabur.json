{
  "builds": [
    {
      "src": "package.json",
      "use": "@zeabur/node",
      "config": {
        "buildCommand": "npm install && npx prisma generate && npm run build && cd client && npm install && npm run build",
        "startCommand": "npm start",
        "nodeVersion": "18"
      }
    }
  ]
}